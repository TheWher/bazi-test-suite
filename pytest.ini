[pytest]
testpaths = tests
pythonpath = bazi-paipan
timeout = 300
markers =
    smoke: 冒烟测试 — 核心路径快速验证
    engine: 排盘引擎精度测试
    agent: Agent 分析质量测试
    blind: 盲测（需 API Key，成本较高）
    slow: 耗时测试（>30s）
addopts = -v --tb=short --strict-markers
